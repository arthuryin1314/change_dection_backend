# 地物识别功能实施计划（前后端完整版）

> 最后更新：2026-06-09
> 状态：**全部步骤已完成**

---

## 背景

基于已训练完成的 DeepLabV3+ 模型（识别 6 类地物：背景、水系、林地、道路、种植土地、建（构）筑物），在现有 FastAPI 后端中新增推理接口，前端 OpenLayers 地图上以半透明色块叠加展示识别结果。

---

## 整体数据流（实际实现版）

```
前端 ControlPanel 选择影像 → 下拉框触发 getImageDetails(id)
  ↓ 获得 { wms_url, layer_name, bbox, img_path, ... }
前端 loadImageToMap → 解析 wms_url，创建 ImageWMS 图层渲染遥感影像
  ↓
前端 LegendPanel 勾选要显示的地物类别（驱动 classes 参数）
  ↓
前端点击"地物识别"按钮 → identify()
  ↓ calculateSegmentSize(bbox)：按比例计算 width/height（长边=1024，最大4096）
  ↓ POST /api/segment { image_id, bbox, width, height, srs:"EPSG:4326", classes:[1,2,...] }
后端 segment_image()
  ↓ 鉴权：只能查询自己的影像（current_user）
  ↓ 查 DB 得到 img_path，校验文件可读
  ↓ asyncio.to_thread → read_tif_rgb_window(img_path, bbox, srs)
      ├─ rasterio 打开 TIF，CRS 转换 bbox → TIF 原生坐标
      ├─ 计算 Window 交集（处理 bbox 超出影像边界）
      ├─ 像素上限保护：native > 16384px 时等比缩小读取
      └─ 返回 TifReadResult（含 PIL Image、偏移量、尺寸信息）
  ↓ asyncio.to_thread → deeplab_service.segment_rgba_png(pil_image, classes)
      ├─ SlidingWindowPredictor：512×512 tile，128px overlap
      ├─ 每块缩放→推理→argmax → class mask
      └─ 按 classes 参数过滤类别，生成 RGBA PNG（alpha=180/透明）
  ↓ asyncio.to_thread → _compose_and_resize_result_png()
      ├─ 处理部分重叠：将分割结果贴到全 bbox 画布的正确位置
      └─ 缩放到前端请求的 width×height
  ↓ base64 编码，返回 { image: "data:image/png;base64,...", bbox }
前端收到结果
  ↓ transformExtent(bbox, EPSG:4326 → EPSG:3857)
  ↓ 创建 ImageStatic 图层，imageExtent 精确对齐
  ↓ 叠加在 WMS 影像图层之上（zIndex:10）
LegendPanel 透明度滑块 → watch(resultOpacity) → resultLayer.setOpacity()
```

---

## 实施步骤

### Step 1：整合 DeepLab 代码到后端项目 ✅

**操作**：从 `E:\change_detection\deeplabv3-plus-pytorch-main` 复制到后端项目根目录。

| 来源 | 目标位置 | 说明 |
|------|---------|------|
| `nets/` | `change_detection_backend/nets/` | 模型网络定义（Xception backbone 等） |
| `deeplab.py` | `change_detection_backend/deeplab.py` | DeeplabV3 推理类 |
| `model_data/` | `change_detection_backend/model_data/` | backbone 预训练权重 |
| `logs/.../best_epoch_weights.pth` | `change_detection_backend/weights/best_epoch_weights.pth` | 训练好的模型权重 |

**注意**：`deeplab.py` 中 `model_path` 已修改为 `weights/best_epoch_weights.pth`。

---

### Step 2：新建 `utils/tif_reader.py` ✅

> **与原计划的偏差**：原计划通过 GeoServer WMS 拉取影像，**实际改为直接用 `rasterio` 读取 TIF 文件**，性能更好，不依赖 GeoServer 在线服务。

**核心逻辑**（`utils/tif_reader.py`）：

```
read_tif_rgb_window(tif_path, bbox, srs)
├─ 将请求 bbox 从 srs（如 EPSG:4326）转换到 TIF 原生 CRS
├─ 计算 rasterio Window，与文件全图求交集
├─ _cap_dimensions()：单边超 NATIVE_PIXEL_CAP(16384) 时等比缩小，防内存溢出
├─ ds.read([1,2,3], window=...) 读出 RGB 数组
└─ 返回 TifReadResult（PIL Image + 偏移量/尺寸元信息，供后续合成用）
```

**错误类型**：
- `UnsupportedSrsError`：无法解析 SRS
- `NoOverlapError`：bbox 与影像无重叠

---

### Step 3：新建 `utils/predict_large_image.py` ✅

> **与原计划的偏差**：原计划直接调用 DeeplabV3 单次推理，**实际新增滑动窗口分块推理**，支持超大遥感影像。

**`SlidingWindowPredictor`**（`utils/predict_large_image.py`）：

| 参数 | 值 | 说明 |
|------|-----|------|
| `tile_size` | 512 | 每块推理尺寸（与训练尺寸一致） |
| `overlap` | 128 | 相邻块重叠像素 |
| `stride` | 384 | 步长（= tile_size - overlap） |

**`_predict_tile(tile)` 流程**：
1. 缩放到 512×512
2. 归一化预处理
3. 模型推理（`torch.no_grad()`，支持 CUDA）
4. softmax → argmax → class mask（uint8）

---

### Step 4：新建 `utils/deeplab_service.py` ✅

**单例封装**，FastAPI 启动时加载一次，避免每次请求重复加载。

```python
_model: Optional[DeeplabV3] = None

def load_model() -> None          # 在 lifespan 中调用
def get_model() -> DeeplabV3      # 在接口中获取实例

def segment_rgba_png(pil_image, classes=None) -> bytes:
    # 调用 _predict_mask → 获得 (H,W) class-index mask
    # 按 model.colors 映射 RGB
    # classes=None → 所有前景类 alpha=180；background alpha=0
    # classes=[1,3] → 只渲染指定类，其余 alpha=0
    # 返回 RGBA PNG bytes
```

---

### Step 5：新建 `router/segment.py` ✅

**接口**：`POST /api/segment`

**请求体（`SegmentRequest`）**：

```json
{
  "image_id": 1,
  "bbox": "116.3,39.8,116.5,40.0",
  "width": 1024,
  "height": 768,
  "srs": "EPSG:4326",
  "classes": [1, 2, 3, 4, 5]
}
```

| 字段 | 校验规则 |
|------|---------|
| `image_id` | gt=0 |
| `bbox` | 4个数字，minx<maxx，miny<maxy |
| `width/height` | 1 ≤ 值 ≤ 4096 |
| `srs` | 必须为 `EPSG:\d{4,6}` |
| `classes` | 每个值 1-5，自动去重；null 表示渲染全部前景类 |

**处理流程**：

```
1. 鉴权：get_current_user，确认影像属于当前用户
2. 查 DB：get_image_by_id → 获取 img_path
3. 校验文件：os.path.isfile + os.access(R_OK)
4. to_thread → read_tif_rgb_window(img_path, bbox, srs)  → TifReadResult
5. to_thread → deeplab_service.segment_rgba_png(image, classes) → RGBA PNG bytes
6. to_thread → _compose_and_resize_result_png(png, tif_result, width, height)
   ├─ 处理部分重叠偏移：将分割结果贴到全 bbox 画布
   └─ 最终 resize 到请求的 width×height
7. base64 编码，返回
```

**响应体**：

```json
{
  "code": 200,
  "message": "识别成功",
  "data": {
    "image": "data:image/png;base64,...",
    "bbox": "116.3,39.8,116.5,40.0"
  }
}
```

---

### Step 6：修改 `main.py` ✅

```python
@asynccontextmanager
async def lifespan(app):
    async with async_engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)   # DDL 建表
    await asyncio.to_thread(deeplab_service.load_model)  # 加载 DeepLab
    images.start_tmp_cleanup_task()                       # 临时文件清理
    try:
        yield
    finally:
        await images.stop_tmp_cleanup_task()

app.include_router(segment.router)  # 注册 /api/segment
```

---

### Step 7：前端 API 层 ✅

**`src/api/segment.js`**：

```js
export function segmentImage(data) {
  return request({
    url: '/segment',
    method: 'post',
    data,
    timeout: 300000,   // 5分钟超时（推理耗时较长）
  })
}
```

HTTP 基础配置（`utils/request.js`）：
- `baseURL: '/api'`
- 请求拦截器：自动附加 `Authorization: Bearer <token>`
- 响应拦截器：401 自动跳转登录，非静默错误弹 ElMessage

---

### Step 8：前端路由 ✅

**`src/router/modules/identification.js`**：

```js
{
  path: 'identification',
  name: 'Identification',
  meta: { title: '地物识别', keepAlive: true, componentName: 'IdentificationIndexView' },
  component: () => import('@/views/identification/index.vue'),
}
```

`keepAlive: true`：切换路由时保持地图实例，回来后执行 `map.updateSize()` 刷新尺寸。

---

### Step 9：前端识别页主组件 ✅

**`src/views/identification/index.vue`**

**关键常量**：

| 常量 | 值 | 说明 |
|------|-----|------|
| `SEGMENT_LONG_SIDE` | 1024 | 请求分辨率长边 |
| `SEGMENT_MAX_SIZE` | 4096 | 单边最大像素 |
| `SEGMENT_CLASS_ID_MAP` | `{water:1, forest:2, road:3, planting:4, buildingStructure:5}` | 图例名到类别ID映射 |

**地图初始化（`initMap()`）**：
- 天地图矢量底图（`vec_w`）+ 注记层（`cva_w`）
- 关闭默认 attribution/zoom/rotate 控件

**影像加载（`loadImageToMap(currentImage)`）**：
- `parseWmsConfig()`：解析 `wms_url`，提取 GeoServer 路径段，拼接 `workspace:layerName`
- 创建 `ImageWMS` 图层（crossOrigin: anonymous，serverType: geoserver）
- `fitToImageBbox()`：`view.fit()` 动画缩放到影像范围，记录 `imageCenter/imageZoom`

**地物识别（`identify(selectedImage)`）**：
- `segmentRequestSeq++`：序号机制防止旧请求覆盖新结果（类 CancelToken）
- `calculateSegmentSize(bbox)`：等比计算，长边=1024，最大4096
- `selectedClassIds`：computed，从 `checkedLegends` 映射为 `[1,2,3,...]`，发给后端
- 结果图层：`transformExtent(bbox, EPSG:4326→EPSG:3857)` → `ImageStatic`（zIndex:10）
- `watch(resultOpacity)` → `resultLayer.setOpacity(val)` 实时调节

**生命周期**：
- `onMounted`：初始化地图
- `onActivated`：`keepAlive` 激活时 `updateSize()`
- `onBeforeUnmount`：`destroyMap()` 清理地图实例和图层

---

### Step 10：前端控制面板组件 ✅

**`src/views/identification/component/controlPanel.vue`**

| 功能 | 实现 |
|------|------|
| 影像下拉选择 | `el-select`，`onMounted` 调 `getImageList()` 填充选项 |
| 选中影像 | `handleChange(id)` → `getImageDetails(id)` → emit `selectImage` |
| 清除选择 | `@clear` → emit `selectImage(null)` |
| 地物识别按钮 | `:loading="isIdentifying"` 显示加载态，click emit `identify` |
| 清除结果按钮 | `v-if="hasResult"` 条件显示，click emit `clear-result` |

Props：`isIdentifying`（Boolean），`hasResult`（Boolean）
Emits：`selectImage`、`identify`、`clear-result`

---

### Step 11：前端图例面板组件 ✅

> **与原计划的偏差**：原计划仅提到透明度滑块，**实际实现了完整的图例+类别过滤面板**，图例勾选直接驱动后端 `classes` 参数。

**`src/views/identification/component/legendPanel.vue`**

**图例配置**：

| 类别值 | 颜色 | 显示名 |
|--------|------|--------|
| `buildingStructure` | `#d32f2f` 红 | 房屋建筑 |
| `water` | `#1976d2` 蓝 | 水系 |
| `planting` | `#fbc02d` 黄 | 种植土地 |
| `forest` | `#388e3c` 绿 | 林地 |
| `road` | `#6b7280` 灰 | 道路 |
| `other` | `#d1d5db` 浅灰 | 其他 |

**交互**：
- 全选 checkbox（含 indeterminate 中间态）
- 每类独立勾选 → v-model `checkedLegends`（传至父组件 → 计算 `selectedClassIds` → POST 时附带）
- 透明度滑块（仅 `hasResult=true` 时显示）：step=0.05，range 0-1 → v-model `opacity`

---

## 涉及文件清单

### 后端（新增/修改）

| 文件 | 操作 | 说明 |
|------|------|------|
| `nets/` | 新增（复制） | DeepLab 网络定义 |
| `deeplab.py` | 新增（复制+改路径） | 推理类，model_path → weights/ |
| `model_data/` | 新增（复制） | backbone 预训练权重 |
| `weights/best_epoch_weights.pth` | 新增（复制） | 训练权重 |
| `utils/tif_reader.py` | **新增** | rasterio 直读 TIF，CRS 转换，像素上限保护 |
| `utils/predict_large_image.py` | **新增** | SlidingWindowPredictor 滑动窗口推理 |
| `utils/deeplab_service.py` | 新增 | DeepLab 单例封装，支持 classes 过滤 |
| `router/segment.py` | 新增 | POST /api/segment，含合成/缩放逻辑 |
| `main.py` | 修改 | lifespan 加载模型+建表，注册 segment router |

### 前端（新增/修改）

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/api/segment.js` | 新增 | `segmentImage()` POST，timeout=300s |
| `src/router/modules/identification.js` | 新增 | 路由配置，keepAlive=true |
| `src/views/identification/index.vue` | 新增 | 主页面：地图、WMS图层、识别逻辑、序号防竞争 |
| `src/views/identification/component/controlPanel.vue` | 新增 | 影像选择、识别/清除按钮 |
| `src/views/identification/component/legendPanel.vue` | **新增** | 图例+类别过滤+透明度滑块 |

---

## 与原计划的主要偏差

| 原计划 | 实际实现 | 原因 |
|--------|---------|------|
| 向 GeoServer 发 WMS 请求拉取影像 | 直接用 rasterio 读本地 TIF 文件 | 不依赖 GeoServer 在线，速度更快 |
| 单次整图 DeepLab 推理 | 滑动窗口推理（512×512，128px overlap） | 支持超大遥感影像，避免内存溢出 |
| 无 bbox 部分重叠处理 | `_compose_and_resize_result_png()` 处理偏移+合成 | bbox 可能超出影像边界 |
| 前端透明度滑块 | 图例面板含图例+类别过滤+透明度三合一 | 类别过滤需求驱动后端 classes 参数 |
| 前端尺寸固定 800×600 | 按 bbox 比例动态计算（长边1024，max 4096） | 保持地理比例，避免拉伸变形 |

---

## 暂不做的事

- 识别结果不入库（`detection_tasks` 表暂不写入），后续有历史记录需求再加
- 结果不发布到 GeoServer（base64 直接返回，响应更快）
- 不支持整幅大图整体推理（只推理当前 bbox 区域，避免内存溢出）
- 导出结果按钮（页面已有占位，功能未实现）

---

## 依赖要求

### 后端

conda 环境 `deeplabv3` 中已安装：

| 包 | 说明 |
|----|------|
| `torch 2.12.0.dev+cu128` | 支持 RTX 5060 Ti |
| `torchvision` | 图像变换 |
| `rasterio` | TIF 文件读取、CRS 转换 |
| `Pillow` | 图像处理 |
| `numpy` | 数组操作 |
| `opencv-python` | 分块预测中的 resize |

FastAPI 启动需使用 `deeplabv3` 环境运行（包含 torch+rasterio）。

### 前端

| 包 | 说明 |
|----|------|
| `ol`（OpenLayers） | `ImageWMS`、`ImageStatic`、`transformExtent` |
| `element-plus` | UI 组件（Select、Button、Card、Checkbox、Slider） |
| `axios` | HTTP 请求，request.js 封装 |
| `@vueuse/core` | （如需响应式工具） |
