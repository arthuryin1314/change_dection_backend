# 地物识别与变化分析

本上下文描述影像的地物分类及两期地物类型变化。以下矩阵定义、统计范围、自动处理流程和面积单位已于 2026-09-05 经用户确认。

## 领域术语

**地物类型转移面积矩阵**：六行六列的面积表，行表示前一期地物类别，列表示后一期地物类别。每个单元格表示对应类别转化的面积，对角线表示类别未变的面积；类别为水系、林地、道路、种植土地、房屋建筑、其他／背景。

**地物识别结果**：对影像各像素的地物分类及其空间位置的记录，是分类面积统计和两期转移面积计算的依据；彩色叠加图是其展示形式，各类总面积不能替代逐像素分类结果。

## 统计范围

- 单期地物识别统计整幅影像有效数据范围内的各类面积。
- 两期变化矩阵只统计两期都有有效数据的空间重叠区域，不要求两幅影像的覆盖范围完全一致。
- 未覆盖区域和无效数据不作为“其他／背景”参与转移统计；“其他／背景”是有效像素的分类类别。

## 自动处理与结果复用

- 用户选择前后两期影像和一个地物识别模型，点击“变化检测”。
- 两期均使用所选模型版本的识别结果；已有符合条件的结果直接复用，缺失的自动识别并保存，再计算转移面积矩阵。
- 用户无需先到地物识别页面手动完成两次识别。首次计算可能比复用已有结果耗时更长。

## 面积单位

2026-09-08 确认展示位置：地物识别主页面保持识别与色块展示，不新增六类面积面板。变化检测页面提供变化前／变化后两个区域，各自选择影像，沿用两期共用模型，按完整身份只读关联已有分类结果并显示各自整幅有效区域面积；不直接选择结果记录。矩阵仍只统计共同有效区域。仅缺面积汇总时可单独统计保存栅格，不重跑模型；缺失分类的自动识别由发起变化检测后的补算流程承担。历史列表与已确认的单期历史详情保留。

- 后端统一按平方米计算和保存面积。
- 前端单期分类面积和转移面积矩阵默认显示公顷，支持切换平方米、平方公里；1 公顷等于 10,000 平方米，1 平方公里等于 1,000,000 平方米。
- 单位切换仅改变展示，不重新执行识别或变化分析。

## 变化检测历史记录

- 变化检测完成后保存转移面积矩阵，以及前后两期影像、所用模型版本、统计范围和计算时间。
- 变化检测页面提供历史记录入口，可直接打开已保存结果查看，无需重新计算。
- 用户进一步确认：本版也提供单期地物识别历史列表入口，可打开已保存分类图、六类面积及影像和模型版本信息；不仅提供按结果标识读取的接口。

## 空间对齐

- 首版要求输入的两期影像已经完成位置配准，系统不自动纠正影像本身的位置偏移。
- 系统自动统一坐标系、像素大小和像素网格，然后在共同有效区域计算转移面积。
