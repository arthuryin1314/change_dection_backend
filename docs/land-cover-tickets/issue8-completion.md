Issue #8 已完成，前后端成果均已推送至 `feature-compute-area` 分支。本次按用户授权正式关闭；以下验证结果依据 issue8 实施任务的最终测试与复审记录，两端远端 HEAD 已由总指挥再次核对。

### 交付提交

- 后端远端 HEAD：`fe5c734`。包含 `4496447`（像元面积轴契约）、`8010278`（面积汇总、迁移、只读 resolve、并发／持久化）、`b18b5d1`（resultId + identitySha256 固定及回滚后 ORM 访问修复）。
- 前端远端 HEAD：`ed8915b`。包含 `652af5d`（变化页两期面积）、`dbe1a25`（身份校验及错误详情）、`ed8915b`（共享 API 错误解析）。前端仓库现为 `arthuryin1314/change_detection`。

### 验证结果

- 后端完整测试：174 passed、1 skipped；随后显式 PostgreSQL 端到端测试：1 passed。
- 前端：64 tests、vue-tsc、Oxlint、ESLint、Vite build 均通过。
- Spec 与 Standards 最终复审均无发现。
- 真实 28,311×26,426、EPSG:4528 栅格：统计耗时 3.867s，峰值内存 195.8 MiB。
- 页面选取 2024／2025 两条结果后，从分类栅格重新计算，与数据库保存值逐项最大差异均为 0 m²。

### 功能口径与后续

按每个地物类别汇总整幅有效区域面积，排除 NoData，保留有效背景；分类精度由语义分割模型负责。面积位于变化检测页前后两个区域，影像选择关联已有结果。

#8 从待办调度中移除；保留 #9 及后续任务的依赖关系。完成依据为上述功能分支交付与验证，不表示已合并默认分支或部署上线。
