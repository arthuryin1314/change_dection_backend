验收结论：通过。Issue #13 已实现，自动统一空间网格与真实异图链路已完成验收。

## 交付状态

后端本地提交 f03bf1e、前端本地提交 9c57c5c；两端 feature-compute-area 均已包含各自 issue9 和 issue13 的代码。当前远端 feature-compute-area 仍为后端 fe5c734、前端 ed8915b，因此本评论不表示新代码已经推送、合并默认分支或部署。本次仅按用户要求记录完成并关闭 Issue，不执行推送。

## 验证依据

以下自动化与真实性能数据来自已提交的 docs/issue-13-grid-alignment-acceptance.md；本次收尾只读核对记录与本地提交，没有重新执行测试。

- 后端常规测试 207 passed、3 skipped（需显式启用的 PostgreSQL 集成）；#13 PostgreSQL 集成单独执行 2 passed。
- 前端 75 passed；vue-tsc、Oxlint、ESLint、Vite build 全部通过。
- 不同 CRS、分辨率和原点的真实小 GeoTIFF 从 API 入口计算；独立面积基准、窗口不变性、前后交换以及旧记录迁移等测试覆盖对齐契约。
- 真实 2024/2025 两份分类结果分别为 21,141×22,649、1.0 m 和 26,426×28,311、约 0.8 m；两者均 EPSG:4528。分析网格为 EPSG:4528、1×1 m，共同有效面积 478,655,959.1780932 m²，约 47,865.5959 公顷。
- 真实异图纯计算 30.3333722 秒，峰值 RSS 228,331,520 bytes（约 217.754 MiB），符合已定 120 秒／512 MiB 门槛。最终真实 POST 用时 32.2134743 秒，GET 200，矩阵、网格与分析信息读回一致。

## 页面证据的区分

验收文档原先记录受控响应驱动的页面验证，以及独立真实 API 验证。之后用户又在实际 localhost:5173/changeDetection/change 页面选择真实 2024/2025 影像，并提供检测和交换前后两轮截图；这部分用户验收由 issue13 实施任务转达，独立于受控响应测试。

用户页面显示双方结果已匹配、完整 6×6 矩阵、EPSG:4528、1×1 分辨率。交换后参考网格从“变化前”变为“变化后”，共同有效面积保持一致。转置核对例：原“其他／背景→水系”0.2106 km²，交换为“水系→其他／背景”21.0628 ha；原“种植土地→房屋建筑”20.1101 km²，交换为“房屋建筑→种植土地”2011.008 ha，差异符合页面单位与舍入精度。完整转置性质另有自动化测试支持；不将显示舍入值声称为逐位相等。

## 保留边界

输入影像仍需已配准；不自动纠正内容位置偏移。矩阵依据统一网格及共同有效掩膜统计，与原生网格单期面积可能不同。长计算保留目录锁，竞争操作可能收到 IDENTIFICATION_RESULT_BUSY；文档已记录该行为。主包超过 500 kB 的既有构建提示仍存在。

本次完成为功能分支实现与验收完成，不代表已经发布新代码。
